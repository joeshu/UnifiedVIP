## 变更说明

请勾选与本 PR 相关的项：

- [ ] 修改了 `urlPattern`
- [ ] 已同步更新 `mitmHosts`（如适用）
- [ ] 已运行 `npm run validate`
- [ ] 已运行 `npm run check:mitm`
- [ ] 已运行 `npm run build` 并确认 `dist/rewrite.conf` 变更符合预期

## MITM 变更摘要

- 新增 host:
- 删除 host:
- 说明:

## 验证结果

粘贴关键输出（可选）：

```text
npm run validate
npm run check:mitm
```
