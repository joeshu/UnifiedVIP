# UnifiedVIP
# Unified VIP Unlock Manager v22

自动构建的 VIP 解锁脚本。

## 使用

订阅规则：https://raw.githubusercontent.com/你的用户名/vip-unlock-configs/main/dist/rewrite.conf

---

## 上传步骤

1. **创建 GitHub 仓库** `vip-unlock-configs`
2. **在本地创建文件夹** 和上面相同的结构
3. **复制所有文件内容** 保存到对应路径
4. **执行命令**：
```bash
cd vip-unlock-configs
git init
git add .
git commit -m "init: v22.0.0"
git remote add origin https://github.com/你的用户名/vip-unlock-configs.git
git branch -M main
git push -u origin main
